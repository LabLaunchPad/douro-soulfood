# Backlog

Not-yet-started tasks live here as OKF files, using the templates in `.ai/tasks/templates/`. Promote a task to `.ai/tasks/active/` when work begins on it.

## Known follow-ups (from prior sessions' audits, not yet task-ified)
- ~~`/menu` fails the Lighthouse performance budget~~ — **done**: image payload cut 80% (4.18MB → 828KB, PR #34) and DOM size cut 28% (1526 → 1092 elements, PR #35). See `benchmarks/reports/MENU-IMAGE-FIX.okf.md` and `benchmarks/reports/MENU-DOM-SIZE-FIX.okf.md`. Honest caveat: the Lighthouse score itself remained noisy in this sandbox even after both real fixes — treat any single score reading here with the documented skepticism, not as ground truth.
- ~~Fix `Base.astro`'s inline JSON-LD/CSP gap~~ — **done, and turned out more severe than documented** (PR #37): CSP's `script-src 'self'` was silently blocking ALL 5 inline scripts on the site, not just JSON-LD — **this meant the mobile hamburger menu and the Google Maps consent-gate button did not work in production**, confirmed via a real headless-Chrome click test before/after. Fixed via SHA-256 hash allowlisting. New verification script: `node scripts/checks/verify-csp-hashes.mjs`. See `.ai/packs/security.okf.md`.
- ~~Add Playwright coverage for `/about`, `/catering`, `/contact`~~ — **done** (PR #36): `tests/{about,catering,contact}.spec.ts` added, 76 → 134 total tests.
- **Convert raw `public/images/` assets to `src/assets/` for real image-pipeline compression gains** — explicitly deferred, not silently skipped. This requires changing the Keystatic content-collection `image` field's schema type, which risks affecting how the business owner edits menu items in the CMS admin UI — one of `AGENTS.md`'s own standing stop conditions. User was asked directly and chose to skip it (see `.ai/memory/human-approvals.md`), since the direct image-resize fix (PR #34) already captured the large practical win without this risk. Do not attempt without a fresh, explicit ask.
- PR #20 (Impressum/Datenschutz legal pages) — blocked on business-owner-supplied legal facts, not actionable by an agent.
