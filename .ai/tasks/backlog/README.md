# Backlog

Not-yet-started tasks live here as OKF files, using the templates in `.ai/tasks/templates/`. Promote a task to `.ai/tasks/active/` when work begins on it.

## Known follow-ups (from prior sessions' audits, not yet task-ified)
- ~~`/menu` fails the Lighthouse performance budget~~ — **done**: image payload cut 80% (4.18MB → 828KB) and DOM size cut 28% (1526 → 1092 elements). See `benchmarks/reports/MENU-IMAGE-FIX.okf.md` and `benchmarks/reports/MENU-DOM-SIZE-FIX.okf.md`. Honest caveat: the Lighthouse score itself remained noisy in this sandbox even after both real fixes — treat any single score reading here with the documented skepticism, not as ground truth.
- **a 64KB `Footer.*.css` chunk is render-blocking on every route** (440–750ms estimated FCP/LCP savings depending on page) — investigated: Lighthouse's `unused-css-rules` audit scores a perfect 1.0/zero wasted bytes, meaning this is legitimate, fully-used Tailwind output, not bloat. Not a bug; no fix attempted. Would require critical-CSS extraction tooling (a new dependency) for a modest, likely sandbox-noise-swamped gain — not worth the added complexity/risk without a clearer signal from an unrestricted environment.
- ~~Fix `Base.astro`'s inline JSON-LD/CSP gap~~ — **done, and turned out more severe than documented**: CSP's `script-src 'self'` was silently blocking ALL 5 inline scripts on the site, not just JSON-LD — **this meant the mobile hamburger menu and the Google Maps consent-gate button did not work in production**, confirmed via a real headless-Chrome click test before/after. Fixed via SHA-256 hash allowlisting. New verification script: `node scripts/checks/verify-csp-hashes.mjs`. See `.ai/packs/security.okf.md`.
- ~~Add Playwright coverage for `/about`, `/catering`, `/contact`~~ — **done**: `tests/{about,catering,contact}.spec.ts` added, 76 → 134 total tests.
- **Convert raw `public/images/` assets to `src/assets/` for real image-pipeline compression gains** — explicitly deferred, not silently skipped. Requires changing the Keystatic content-collection `image` field's schema type, which risks affecting how the business owner edits menu items in the CMS admin UI — one of `AGENTS.md`'s own standing stop conditions. User was asked directly and chose to skip it (see `.ai/memory/human-approvals.md`), since the direct image-resize fix already captured the large practical win without this risk. Do not attempt without a fresh, explicit ask.
- ~~PR #20 (Impressum/Datenschutz legal pages)~~ — **merged**. The pages are live; only the legal-form/UID/Firmenbuchnummer fields remain bracketed placeholders (business-owner-supplied facts, not agent-actionable). The stale EU ODR platform reference was also since removed (platform discontinued 2025-07-20).
- ~~Self-host Google Fonts~~ — **done**: `.woff2` under `public/fonts/`, no request to Google's servers.
- ~~Consent-gate Google Maps~~ — **done**: `MapEmbed.astro` implements the two-click pattern.
- ~~Typography token scale had zero adoption~~ — **done**: realigned `--text-*` values to match the real, disciplined Tailwind-utility usage pattern (298 call sites, zero visual change).
- ~~44 un-tokenized `stone-*`/`amber-*`/`zinc-*` call sites~~ — **done for 38**; remaining 6 amber accents verified as correctly context-specific, not a gap. See `docs/design-system/COLOR_SYSTEM.md`.
- **Spacing (75 half-step Tailwind utility instances)** — investigated, deliberately left as-is (defensible for compact UI, not drift). See `docs/design-system/SPACING_SYSTEM.md`.
- **CI's `@axe-core/playwright` accessibility gate only covers 2 of 7 routes** — real, not-yet-actioned gap. Widening it is a legitimate next task.
