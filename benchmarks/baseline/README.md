# benchmarks/baseline/

Empty as of this setup — no historical benchmark data exists yet. The first real tool run against a live, reachable URL becomes the baseline candidate. See `benchmarks/README.md`.
