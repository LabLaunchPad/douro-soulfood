---
export const prerender = true;

import Base from '@/layouts/Base.astro';
import NavBar from '@/components/layout/NavBar.astro';
import HeroSection from '@/components/sections/HeroSection.astro';
import ReviewBadge from '@/components/ui/ReviewBadge.astro';
import CategoryIcon from '@/components/ui/CategoryIcon.astro';
import FeatureCard from '@/components/sections/FeatureCard.astro';
import UserReviews from '@/components/sections/UserReviews.astro';
import MapEmbed from '@/components/ui/MapEmbed.astro';
import Footer from '@/components/layout/Footer.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import siteSettings from '@/content/settings/default.json';

const { address_line1, city, postal_code, phone, hours } = siteSettings;
const phoneHref = `tel:${phone.replace(/[^+\d]/g, '')}`;

/**
 * HOME PAGE — D'ouro Soulfood Bistro
 */

/** 6 Core Category Slots for Homepage */
const categorySlots = [
  { slug: 'appetizers', title: 'Vorspeisen / Entradas', desc: 'Coxinha, Pão de Queijo & Ceviche', icon: 'appetizers' },
  { slug: 'quesadillas', title: 'Quesadillas', desc: 'Melted Gouda & Mozzarella-Mix', icon: 'quesadillas' },
  { slug: 'tacos', title: 'Tacos', desc: 'Picanha, Pollo, Vegano & Gambas', icon: 'tacos' },
  { slug: 'bowls', title: 'Bowls', desc: 'Brazil Beef, Chipotle & Surf & Turf', icon: 'bowls' },
  { slug: 'mains', title: 'Pratos Feitos', desc: 'Feijoada & African Peanut Stew mit Fufu', icon: 'mains' },
  { slug: 'drinks', title: 'Bebidas & Sobremesas', desc: 'Caipirinha, Guaraná & Brigadeiros', icon: 'drinks' },
];

/** 9 Popular dishes photos (image-only gallery layout). width/height match the actual source files (300×300). */
const popularDishes = [
  { src: '/images/menu/taco-especial.png', alt: 'Taco Especial', width: 300, height: 300 },
  { src: '/images/menu/quesadilla-haehnchen.png', alt: 'Quesadilla Hähnchen', width: 300, height: 300 },
  { src: '/images/menu/feijoada.png', alt: 'Feijoada Brasileira', width: 300, height: 300 },
  { src: '/images/menu/pao-de-queijo.png', alt: 'Pão de Queijo', width: 300, height: 300 },
  { src: '/images/menu/mango-ceviche.png', alt: 'Mango Ceviche', width: 300, height: 300 },
  { src: '/images/menu/coxinha.png', alt: 'Coxinha', width: 300, height: 300 },
  { src: '/images/menu/african-stew.jpg', alt: 'African Peanut Stew', width: 300, height: 300 },
  { src: '/images/menu/brigadeiros.jpg', alt: 'Brigadeiros', width: 300, height: 300 },
  { src: '/images/menu/brisa-do-mar.png', alt: 'Brisa do Mar', width: 300, height: 300 },
];

/** 9 Photos for Galerie section. width/height match each source file's actual dimensions. */
const galleryImages = [
  { src: '/images/gallery/1.webp', alt: 'D\'ouro Bistro Spezialität 1', width: 680, height: 510 },
  { src: '/images/gallery/2.webp', alt: 'D\'ouro Bistro Spezialität 2', width: 594, height: 510 },
  { src: '/images/gallery/3.webp', alt: 'D\'ouro Bistro Spezialität 3', width: 382, height: 510 },
  { src: '/images/gallery/4.webp', alt: 'D\'ouro Bistro Spezialität 4', width: 382, height: 510 },
  { src: '/images/gallery/5.webp', alt: 'D\'ouro Bistro Spezialität 5', width: 382, height: 510 },
  { src: '/images/gallery/6.webp', alt: 'D\'ouro Bistro Spezialität 6', width: 382, height: 510 },
  { src: '/images/gallery/7.webp', alt: 'D\'ouro Bistro Spezialität 7', width: 382, height: 510 },
  { src: '/images/gallery/8.webp', alt: 'D\'ouro Bistro Spezialität 8', width: 382, height: 510 },
  { src: '/images/gallery/9.webp', alt: 'D\'ouro Bistro Spezialität 9', width: 680, height: 510 },
];

/** FAQ entries from KeystaticCMS */
const faqEntries = (await getCollection('faq'))
  .sort((a, b) => (a.data.order ?? 0) - (b.data.order ?? 0));

const faqItems = faqEntries.map((item) => ({
  question: item.data.question,
  answer: item.data.answer,
}));

// SEO CONFIG
const SITE_URL = 'https://douro-soulfood.com';
const PAGE_TITLE = "Afro-Lateinamerikanische Küche in Salzburg";
const PAGE_DESCRIPTION =
  "Frische brasilianische & afrikanische Soulfood-Spezialitäten von Angela in Salzburg. Tacos, Bowls, Feijoada & afrikanische Gerichte. Auerspergstraße 10.";
---

<Base
  title={PAGE_TITLE}
  description={PAGE_DESCRIPTION}
  canonical={SITE_URL}
>
  {/* 1. NAVBAR */}
  <NavBar slot="nav" />

  {/* 2. HERO SECTION */}
  <HeroSection
    accent="D'ouro Soulfood Bistro"
    headline="Afro-Latin Soul im Herzen von Salzburg"
    subheadline="Frische, handgemachte brasilianische & afrikanische Spezialitäten von Angela"
    images={[
      {
        src: 'https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=1600&q=80',
        alt: "D'ouro Soulfood Bistro — Afro-Lateinamerikanische Küche in Salzburg",
        width: 1600,
        height: 900,
      },
    ]}
    ctaPrimary={{ label: 'Besuchen Sie uns', href: '/contact' }}
    ctaSecondary={{ label: 'Speisekarte ansehen', href: '/menu' }}
  />

  {/* 3. REVIEW BADGE */}
  <section class="py-2 md:py-3 px-4 md:px-16 border-b border-border-subtle" aria-label="Bewertungen">
    <div class="max-w-7xl mx-auto flex flex-col sm:flex-row items-center justify-center gap-4">
      <ReviewBadge
        rating={4.8}
        reviewCount={978}
        source="Google Reviews"
        href="https://www.google.com/maps/search/?api=1&query=Auerspergstra%C3%9Fe+10%2C+5020+Salzburg%2C+Austria"
        size="lg"
      />
      <span class="text-text-tertiary text-sm">Google Reviews</span>
    </div>
  </section>

  {/* 4. 6 AFRO-LATINO SPEZIALITÄTEN */}
  <section class="pt-4 md:pt-6 pb-3 md:pb-4 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Speisekarte Kategorien">
    <div class="text-center mb-4 md:mb-5">
      <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-1">
        Unsere Speisekarte
      </span>
      <h2 class="text-xl md:text-3xl font-extrabold text-text-primary font-display uppercase tracking-tight">
        Entdecken Sie unsere 6 Afro-Latino Spezialitäten
      </h2>
    </div>

    <div class="grid grid-cols-6 gap-1 sm:gap-3 md:gap-4">
      {categorySlots.map((slot) => (
        <a
          href={`/menu#category-${slot.slug}`}
          class="group flex flex-col items-center text-center p-1.5 sm:p-4 rounded-lg sm:rounded-2xl bg-surface-card border border-border-subtle hover:border-brand-gold/50 hover:bg-surface-elevated transition-all duration-[var(--duration-fast)] no-underline shadow-sm sm:shadow-md min-w-0"
        >
          <div class="w-7 h-7 sm:w-12 sm:h-12 rounded-md sm:rounded-xl bg-brand-gold/15 text-brand-gold flex items-center justify-center mb-1 sm:mb-3 group-hover:scale-110 transition-transform shrink-0">
            <CategoryIcon name={slot.icon} class="w-3.5 h-3.5 sm:w-6 sm:h-6" />
          </div>
          <h3 class="text-[9px] sm:text-sm font-bold text-text-primary group-hover:text-brand-gold font-display transition-colors leading-tight mb-0.5 sm:mb-1 truncate w-full">
            {slot.title.split('/')[0]}
          </h3>
          <p class="hidden sm:block text-[11px] text-text-tertiary leading-tight line-clamp-2">
            {slot.desc}
          </p>
        </a>
      ))}
    </div>
  </section>

  {/* 5. BELIEBTE GERICHTE IM D'OURO BISTRO (REDESIGNED: 9 FOOD PHOTOS ONLY) */}
  <section class="py-3 md:py-5 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Beliebte Gerichte">
    <div class="text-center mb-4 md:mb-5">
      <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-1">
        Empfehlungen
      </span>
      <h2 class="text-xl md:text-3xl font-semibold text-text-primary font-display">
        Beliebte Gerichte im D'ouro Bistro
      </h2>
      <p class="text-text-secondary mt-1 max-w-[75ch] mx-auto text-xs md:text-sm">
        Frisch von Angela zubereitet — mit den besten Zutaten Brasiliens und Westafrikas.
      </p>
    </div>

    {/* 8 Food Photos on Mobile (2 cols), 9 on Desktop (3 cols) */}
    <div class="grid grid-cols-2 lg:grid-cols-3 gap-2.5 sm:gap-4 md:gap-6">
      {popularDishes.map((item, index) => (
        <div class:list={[
          "group relative aspect-square sm:aspect-[4/3] rounded-2xl overflow-hidden bg-surface-card border border-border-subtle hover:border-brand-gold/50 shadow-md transition-all duration-[var(--duration-normal)]",
          index === 8 && "hidden sm:block"
        ]}>
          <Image
            src={item.src}
            alt={item.alt}
            width={item.width}
            height={item.height}
            format="webp"
            quality={85}
            class="w-full h-full object-cover transition-transform duration-[var(--duration-normal)] ease-[var(--ease-out-quart)] group-hover:scale-105"
            loading="lazy"
          />
        </div>
      ))}
    </div>

    {/* CTA: See Full Menu */}
    <div class="mt-4 md:mt-6 text-center">
      <a
        href="/menu"
        class="group inline-flex items-center gap-2 rounded-sm
               bg-brand-gold hover:bg-brand-gold-light active:bg-brand-gold-dark
               min-h-10 md:min-h-12 px-5 py-2.5 md:py-3 text-sm md:text-base font-semibold text-brand-espresso
               focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-gold
               no-underline transition-colors duration-[var(--duration-fast)] ease-[var(--ease-spring)] shadow-glow-gold"
      >
        Komplette Speisekarte ansehen
        <svg
          class="w-4 h-4 opacity-50 group-hover:opacity-100 group-hover:translate-x-0.5 transition-all duration-[var(--duration-fast)] ease-[var(--ease-spring)]"
          viewBox="0 0 256 256"
          fill="none"
          stroke="currentColor"
          stroke-width="24"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <line x1="70" y1="128" x2="216" y2="128" />
          <polyline points="144 56 216 128 144 200" />
        </svg>
      </a>
    </div>
  </section>

  {/* 6. WIE D'OURO BEGANN (TRANSLATED TO GERMAN) */}
  <section class="py-3 md:py-5 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Unsere Geschichte">
    <div class="glass-gold rounded-2xl p-6 md:p-8 max-w-3xl mx-auto text-center border border-brand-gold/20 shadow-lg">
      <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-2">
        Unsere Geschichte
      </span>
      <h2 class="text-xl md:text-3xl font-semibold text-text-primary font-display mb-3">
        Wie D'ouro begann
      </h2>
      <p class="text-sm md:text-base text-text-secondary leading-relaxed max-w-[75ch] mx-auto">
        D'ouro begann während des Lockdowns, als Angela von zu Hause aus selbstgemachtes brasilianisches Essen an alle verteilte, die ein Stück Heimat suchten. Genau diese Wärme und Leidenschaft stecken auch heute in jedem einzelnen Gericht.
      </p>
      <div class="mt-6 flex items-center justify-center gap-3">
        <div class="w-10 h-10 rounded-full bg-brand-gold/20 flex items-center justify-center" aria-hidden="true">
          <svg
            class="w-5 h-5 text-brand-gold"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2" />
            <circle cx="12" cy="7" r="4" />
          </svg>
        </div>
        <div class="text-left">
          <p class="text-xs md:text-sm font-semibold text-text-primary">Angela</p>
          <p class="text-[10px] md:text-xs text-text-tertiary">Gründerin & Chefköchin</p>
        </div>
      </div>
    </div>
  </section>

  {/* 7. FIRMENEVENTS SECTION */}
  <section class="py-2 md:py-3" aria-label="Firmenevents">
    <FeatureCard
      title="Firmenevents"
      description="Von Team-Lunches bis Firmenfeiern — unsere Catering-Pakete passen sich Ihren Bedürfnissen an."
      image={{ src: '/images/catering/event.webp', alt: 'Firmenevent Catering', width: 800, height: 600 }}
      cta={{ label: 'Anfrage senden', href: '/contact' }}
    />
  </section>

  {/* 8. REVIEWS SECTION */}
  <section class="py-2 md:py-3" aria-label="Gästebewertungen">
    <UserReviews />
  </section>

  {/* 9. PRIVATE FEIERN SECTION */}
  <section class="py-2 md:py-3" aria-label="Private Feiern">
    <FeatureCard
      title="Private Feiern"
      description="Geburtstage, Hochzeiten, Familientreffen — wir bringen Soul Food direkt zu Ihnen."
      image={{ src: '/images/catering/private.webp', alt: 'Private Feiern Catering', width: 800, height: 600 }}
      cta={{ label: 'Jetzt planen', href: '/contact' }}
      reverse
    />
  </section>

  {/* 10. GALERIE SECTION (8 PHOTOS ON MOBILE / 9 ON DESKTOP) */}
  <section class="py-3 md:py-5 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Galerie">
    <div class="text-center mb-4 md:mb-5">
      <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-1">
        Impressionen
      </span>
      <h2 class="text-xl md:text-3xl font-semibold text-text-primary font-display">
        Galerie
      </h2>
      <p class="text-text-secondary mt-1 max-w-[75ch] mx-auto text-xs md:text-sm">
        Einblicke in unsere frischen Speisen und das Ambiente im D'ouro Bistro.
      </p>
    </div>

    <div class="grid grid-cols-2 lg:grid-cols-3 gap-2.5 sm:gap-4 md:gap-6">
      {galleryImages.map((img, index) => (
        <div class:list={[
          "group relative aspect-square sm:aspect-[4/3] rounded-2xl overflow-hidden bg-surface-card border border-border-subtle hover:border-brand-gold/50 shadow-md transition-all duration-[var(--duration-normal)]",
          index === 8 && "hidden sm:block"
        ]}>
          <Image
            src={img.src}
            alt={img.alt}
            width={img.width}
            height={img.height}
            format="webp"
            quality={85}
            class="w-full h-full object-cover transition-transform duration-[var(--duration-normal)] ease-[var(--ease-out-quart)] group-hover:scale-105"
            loading="lazy"
          />
        </div>
      ))}
    </div>
  </section>

  {/* 11. FAQ ACCORDION */}
  {faqItems.length > 0 && (
    <section class="py-3 md:py-5 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Häufig gestellte Fragen">
      <div class="text-center mb-4 md:mb-5">
        <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-1">
          FAQ
        </span>
        <h2 class="text-xl md:text-3xl font-semibold text-text-primary font-display">
          Häufig gestellte Fragen
        </h2>
      </div>
      <div class="flex flex-col max-w-4xl mx-auto">
        {faqItems.map((item) => (
          <details class="w-full border-b border-border-subtle group">
            <summary
              class="flex items-center justify-between py-4 cursor-pointer list-none
                     text-text-primary font-medium text-sm md:text-base
                     focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-gold rounded-sm
                     transition-colors duration-[var(--duration-fast)] ease-[var(--ease-spring)] hover:text-brand-gold"
            >
              <span>{item.question}</span>
              <svg
                class="w-4 h-4 md:w-5 md:h-5 text-text-tertiary transition-transform duration-[var(--duration-fast)] ease-[var(--ease-out-quint)] group-open:rotate-180 shrink-0 ml-4"
                viewBox="0 0 20 20" fill="none" stroke="currentColor"
                stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                aria-hidden="true"
              >
                <path d="M5 7.5L10 12.5L15 7.5" />
              </svg>
            </summary>
            <div class="pb-4 text-text-secondary leading-relaxed text-xs md:text-sm">
              {item.answer}
            </div>
          </details>
        ))}
      </div>
    </section>
  )}

  {/* 12. MAP & STANDORT SECTION */}
  <section class="py-3 md:py-5 px-4 md:px-16 max-w-7xl mx-auto" aria-label="Unser Standort">
    <div class="text-center mb-4 md:mb-5">
      <span class="inline-block text-xs md:text-sm font-semibold text-brand-gold uppercase tracking-wider mb-1">
        Standort
      </span>
      <h2 class="text-xl md:text-3xl font-semibold text-text-primary font-display">
        Unser Bistro in Salzburg
      </h2>
    </div>

    <article class="rounded-2xl overflow-hidden bg-surface-glass backdrop-blur-sm border border-border-subtle shadow-lg">
      <div class="flex flex-col md:flex-row">
        {/* Map (consent-gated: real iframe only loads after visitor clicks through) */}
        <div class="w-full md:w-1/2 min-h-[260px] md:min-h-[340px]">
          <MapEmbed
            src="https://maps.google.com/maps?q=Auerspergstra%C3%9Fe%2010,%205020%20Salzburg&t=&z=15&ie=UTF8&iwloc=&output=embed"
            title="D'ouro Soulfood Standort"
            addressLabel="Auerspergstraße 10, 5020 Salzburg"
            class="min-h-[260px] md:min-h-[340px]"
          />
        </div>

        {/* Details */}
        <div class="flex flex-col flex-1 justify-between gap-4 p-5 md:p-8">
          <div class="flex flex-col gap-3">
            <div>
              <span class="text-xs uppercase font-semibold tracking-wider text-brand-gold">Restaurant & Bistro</span>
              <h3 class="text-xl md:text-2xl font-bold text-text-primary font-display mt-0.5">
                Salzburg, Österreich
              </h3>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div class="flex flex-col gap-0.5">
                <span class="text-[11px] text-text-tertiary uppercase font-medium">Adresse</span>
                <address class="not-italic text-xs md:text-sm text-text-secondary leading-relaxed">
                  <p>{address_line1}</p>
                  <p>{postal_code} {city}</p>
                </address>
              </div>
              <div class="flex flex-col gap-0.5">
                <span class="text-[11px] text-text-tertiary uppercase font-medium">Kontakt</span>
                <a
                  href={phoneHref}
                  class="text-xs md:text-sm text-text-secondary hover:text-brand-gold no-underline transition-colors"
                >
                  {phone}
                </a>
              </div>
            </div>
          </div>

          <div class="border-t border-border-subtle pt-4 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3">
            <div>
              <span class="text-[11px] text-text-tertiary block">Öffnungszeiten</span>
              <span
                id="today-hours"
                data-hours={JSON.stringify(hours)}
                class="text-xs md:text-sm font-medium text-text-primary"
              >
                Heute: {hours[0].time} Uhr
              </span>
            </div>
            <a
              href="/menu"
              class="group inline-flex items-center gap-2 rounded-sm bg-brand-gold hover:bg-brand-gold-light active:bg-brand-gold-dark
                     px-4 py-2 text-xs md:text-sm font-semibold text-brand-espresso
                     focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-gold
                     no-underline transition-colors shadow-glow-gold"
            >
              Jetzt bestellen
            </a>
          </div>
        </div>
      </div>
    </article>
  </section>

  {/* 13. FOOTER */}
  <Footer slot="footer" />
</Base>

<script>
  const el = document.getElementById('today-hours');
  const hours = el ? JSON.parse(el.dataset.hours ?? '[]') : [];
  const today = new Intl.DateTimeFormat('de-AT', { timeZone: 'Europe/Vienna', weekday: 'long' }).format(new Date());
  const entry = hours.find((h: { day: string; time: string }) => h.day === today);

  if (el && entry) {
    el.textContent = entry.time === 'Geschlossen' ? 'Heute geschlossen' : `Heute: ${entry.time} Uhr`;
  }
</script>
