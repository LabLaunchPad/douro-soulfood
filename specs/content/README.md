# specs/content/

Structural specs for content, seeded empty in this setup. Populate as specific content need a written spec beyond what `docs/components.md`/`docs/user-flows.md`/`docs/architecture.md` already cover — most content don't need a separate spec file; only add one when there's real complexity worth capturing.
