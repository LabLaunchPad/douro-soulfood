# specs/pages/

Structural specs for pages, seeded empty in this setup. Populate as specific pages need a written spec beyond what `docs/components.md`/`docs/user-flows.md`/`docs/architecture.md` already cover — most pages don't need a separate spec file; only add one when there's real complexity worth capturing.
