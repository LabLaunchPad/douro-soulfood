# specs/flows/

Structural specs for flows, seeded empty in this setup. Populate as specific flows need a written spec beyond what `docs/components.md`/`docs/user-flows.md`/`docs/architecture.md` already cover — most flows don't need a separate spec file; only add one when there's real complexity worth capturing.
