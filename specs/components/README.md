# specs/components/

Structural specs for components, seeded empty in this setup. Populate as specific components need a written spec beyond what `docs/components.md`/`docs/user-flows.md`/`docs/architecture.md` already cover — most components don't need a separate spec file; only add one when there's real complexity worth capturing.
